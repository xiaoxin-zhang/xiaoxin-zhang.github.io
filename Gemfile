source "https://rubygems.org"

# 核心引擎
gem "jekyll", "~> 4.3"
gem "webrick", "~> 1.8"
gem "kramdown-parser-gfm"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
  gem "jekyll-redirect-from"
  gem "jemoji"
  gem "jekyll-gist"       # ← 新增
  gem "jekyll-paginate"   # ← 新增
end

# 去掉这行（除非你确实有依赖需要它）
# gem "connection_pool", "2.5.0"
